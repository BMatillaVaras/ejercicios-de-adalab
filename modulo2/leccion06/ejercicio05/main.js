"use strict";

const input = document.querySelector(".input");

console.dir(input);

//nodeName indica el tipo de elemento html que es 'input'
// required una propiedad que en este caso es false porque no se está utilizando, indica que el campo es obligatorio rellenarlo
// value nos da el valor de lo que hay dentro del input si escribimos algo en él, value recoge lo que se ha escrito. En este caso está vacío ""
